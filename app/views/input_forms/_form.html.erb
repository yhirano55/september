<%= form_with(model: input_form) do |form| %>
  <% if input_form.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(input_form.errors.count, "error") %> prohibited this input_form from being saved:</h2>

      <ul>
        <% input_form.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
